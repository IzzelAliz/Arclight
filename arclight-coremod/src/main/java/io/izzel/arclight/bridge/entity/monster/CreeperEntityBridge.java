package io.izzel.arclight.bridge.entity.monster;

import io.izzel.arclight.bridge.entity.MobEntityBridge;

public interface CreeperEntityBridge extends MobEntityBridge {

    void bridge$setPowered(boolean power);
}
